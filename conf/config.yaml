# conf/config.yaml
defaults:
  - _self_
  - experiment: gpqa  # 🔥 在这里一键切换数据集！(对应 conf/experiment/math_to_hmmt.yaml)
  - optimizer: default        # 🔥 在这里切换优化策略

timestamp: "${now:%Y%m%d_%H%M%S}"

# ================= 路径配置 =================
paths:
  root: "/root/workspace/jychen/ex"
  jsonl: "${paths.root}/outputs/jsonl"
  png: "${paths.root}/outputs/png"
  # 中间产物路径配置保持不变...
  cluster_output: "${paths.jsonl}/{defaults.experiment}_clustered_result.jsonl"
  cluster_summary: "${paths.jsonl}/{defaults.experiment}_cluster_summary.jsonl"
  cluster_plot: "${paths.png}/{defaults.experiment}_cluster_distribution.png"
  cluster_vis: "${paths.png}/{defaults.experiment}_visualization.png"
  freq_file: "${paths.root}/{defaults.experiment}_memory_freq.jsonl"
  optimized_memory: "${paths.root}/{defaults.experiment}_optimized_memory_topk.jsonl"
  rag_cache_dir: "${paths.root}/rag_result_cache"

# ================= 模型配置 =================
model:
  source: "sglang" 
  optimize: "sglang"
  hf_name: "Qwen/Qwen3-4B-Instruct-2507"
  device: "cuda"
  batch_size: 20
  max_input_len: 4096
  max_new_tokens: 2048
  
  gemini_name: "gemini-2.5-flash"
  sglang_api_url: "http://127.0.0.1:30000/v1"
  sglang_model_name: "Qwen/Qwen3-4B-Instruct-2507"
  embedding_name: "BAAI/bge-large-en-v1.5"

# ================= 聚类配置 (相对稳定) =================
cluster:
  method: "agglomerative"
  distance_threshold: 1.0
  kmeans_n_clusters: 10
  vis_method: "tsne"
  enable_pca_preprocess: True
  pca_preprocess_dims: 50
  tsne_perplexity: 40